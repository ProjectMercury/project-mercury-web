//Not sure if an array can be used for the initial object
const initialState = {
  0: [
    {
      id: 1,
      name: '',
      type: 'text'
    }
  ]
};

export const inputReducer = (state = initialState, action) => {};
